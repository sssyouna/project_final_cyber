# PostgreSQL Client Authentication Configuration File for vulnerable setup (M12)
# Allow connections from any IP address without proper restrictions

# TYPE  DATABASE        USER            ADDRESS                 METHOD
# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
# IPv6 local connections:
host    all             all             ::1/128                 trust
# Allow connections from any IP address - VULNERABLE!
host    all             all             0.0.0.0/0               trust
# Allow connections from any IPv6 address - VULNERABLE!
host    all             all             ::/0                    trust

# Database administrative login by Unix domain socket
local   all             postgres                                trust